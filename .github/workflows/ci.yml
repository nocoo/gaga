name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  quality:
    uses: nocoo/base-ci/.github/workflows/quality.yml@ad43150de3a2be2fa464b5cd2f921dc4fa9f8f0f
    with:
      runtime-version: "1.4.0"
      install-policy: "project"
      prepare-command: "bun run build"
      typecheck-command: "bun run typecheck"
      lint: false
      unit: false
      disabled-check-reasons: |-
        {
          "lint": "project has no lint script; previous CI used a no-op lint-command",
          "unit": "previous CI used a no-op test-command; no unit suite is wired"
        }
